<%= form_with model: [post, post_comment], url:post_post_comments_path(post), local: false do |f| %>
  <%= f.text_area :comment, rows:'4', placeholder: "コメントを入力", class: "w-100" %>
  <%= f.submit "送信" %>
<% end %>